apiVersion: v1
kind: ConfigMap

metadata:
  name: product-config

data:

  DB_HOST: postgres
  DB_PORT: "5432"
  DB_NAME: productdb

  REDIS_HOST: redis
  REDIS_PORT: "6379"

  KAFKA_BOOTSTRAP_SERVERS: kafka:9092